__pycache__/
*.pyc
.pytest_cache/
# Per-machine settings (see settings.example.json). Never commit the real
# logseqGraph path; every machine keeps its own copy.
settings.json
# Desktop-context collector databases: runtime data, never deployed.
*.db
*.db-wal
*.db-shm
*.db-journal
*.db.lock
